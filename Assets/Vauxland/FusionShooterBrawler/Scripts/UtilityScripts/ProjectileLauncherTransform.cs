using UnityEngine;

namespace Vauxland.FusionBrawler
{
    public class ProjectileLauncherTransform : MonoBehaviour
    {
        // just a script to reference the projectile launcher transform this is only set on the player prefab if not using the launcher on a weapoon

        public Transform launcherTransform;
    }
}

